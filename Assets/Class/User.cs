public class User
{
    public string name;
    public string id;

    // Start is called before the first frame update
    public User(string name, string id){
        this.name = name;
        this.id = id;
    }

}
